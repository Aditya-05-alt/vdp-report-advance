import DateWiseViewsPanel from '@/components/reports/DateWiseViewsPanel';

export const metadata = {
  title: 'Date-wise Views · Admin · SmartAnalytics',
};

export default function AdminDateWiseViewsPage() {
  return <DateWiseViewsPanel title="Date-wise Views" />;
}
